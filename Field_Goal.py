from pickle import GLOBAL
from tkinter import Variable
print("3 Seconds Left,  Field Goal Attempt")
Yard = input("Enter Yardline:")
Yard = float(Yard)

input = yard > 20
input = yard > 30 
input = yard > 40
input = yard > 50
input = yard > 60
input = yard > 70

if input is "= > 20" then(Variable x = 0, y= 1)


    user = input('would you like to enter another phrase? ')